# GenAI CLI — Corporate Profile
# Copy this file to ~/.genai-cli/.env or source it in your shell.
#
# Configuration precedence (highest first):
#   1. CLI flags (--model, --auto-apply, etc.)
#   2. Environment variables (this file)
#   3. Project config:  .genai-cli/settings.yaml
#   4. User config:     ~/.genai-cli/settings.yaml
#   5. Package defaults: config/settings.yaml

# ── Required ─────────────────────────────────────────────────────
# Bearer token from your corporate AI chat platform.
# Get it: browser DevTools → Network tab → any API request → Authorization header.
# Run `genai auth login` to save interactively, or set here for scripted setups.
GENAI_AUTH_TOKEN=

# Base URL of your corporate AI chat API.
GENAI_API_BASE_URL=https://api-genai.example.com

# ── Optional ─────────────────────────────────────────────────────
# Default model (override per-invocation with --model or /model in REPL).
# Available: gpt-5-chat-global, gpt-5-mini-chat-global, gpt-5-nano-chat-global,
#   gpt-4.1-chat-global, gpt-4.1-mini-chat-global, gpt-4.1-nano-chat-global,
#   claude-3-opus-global, claude-sonnet-4-5-global,
#   gemini-2.5-pro-global, gemini-2.5-flash-global, gemini-2.5-flash-lite-global
# GENAI_MODEL=gpt-5-chat-global

# Auto-apply AI code changes without confirmation (true/false).
# GENAI_AUTO_APPLY=false

# Enable verbose output for debugging (true/false).
# GENAI_VERBOSE=false

# Reuse an existing web UI session (paste session UUID from browser).
# GENAI_SESSION_ID=

# ── Additional settings ──────────────────────────────────────────
# Session storage, file limits, security, and other settings are configured
# in settings.yaml (not environment variables). See config/settings.yaml
# for all available options, or run: genai config get <key>
