# GenAI CLI — Google Profile
# Optimized for Gemini: massive 1M context, great for whole-repo analysis.
# Copy to ~/.genai-cli/.env or source in your shell.
#
# See example.env for all available environment variables.

# ── Required ─────────────────────────────────────────────────────
GENAI_AUTH_TOKEN=
GENAI_API_BASE_URL=https://api-genai.example.com

# ── Model ────────────────────────────────────────────────────────
# Gemini 2.5 Pro:        Best for large codebase analysis (1M context)
# Gemini 2.5 Flash:      Fast + cheap with large context (1M context)
# Gemini 2.5 Flash Lite: Cheapest, quick tasks only (1M context)
GENAI_MODEL=gemini-2.5-pro-global
