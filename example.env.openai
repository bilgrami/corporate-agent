# GenAI CLI — OpenAI Profile
# Optimized for GPT-5: strong reasoning, good SEARCH/REPLACE output, 128K context.
# Copy to ~/.genai-cli/.env or source in your shell.
#
# See example.env for all available environment variables.

# ── Required ─────────────────────────────────────────────────────
GENAI_AUTH_TOKEN=
GENAI_API_BASE_URL=https://api-genai.example.com

# ── Model ────────────────────────────────────────────────────────
# GPT-5:      Best overall coding, review, debugging (128K context)
# GPT-5 Mini: Faster, cheaper, good for simple tasks (128K context)
# GPT-5 Nano: Fastest, cheapest, quick Q&A only (128K context)
# GPT-4.1:    Largest context (1M), good for huge codebases
GENAI_MODEL=gpt-5-chat-global
